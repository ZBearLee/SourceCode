import {Store,install} from './store'
import {mapState,mapGetters,mapMutations,mapActions} from './helpers'
export default{
    Store,
    install,
    mapState,
    mapGetters,
    mapMutations,
    mapActions,
    version:'2.2.1'
}